---
title: Шифрование Office 365 для Скайп, OneDrive, SharePoint и Exchange
ms.author: robmazz
author: robmazz
manager: laurawi
ms.date: 8/21/2018
audience: ITPro
ms.topic: article
ms.service: Office 365 Administration
localization_priority: None
search.appverid:
- MET150
ms.collection: Strat_O365_Enterprise
description: 'Сводка: Описание шифрования для Скайп, OneDrive, SharePoint и Exchange Online.'
ms.openlocfilehash: 5b839b8d290306f2334d3ca3278d0d5dac20a56f
ms.sourcegitcommit: 36c5466056cdef6ad2a8d9372f2bc009a30892bb
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 08/27/2018
ms.locfileid: "22534859"
---
# <a name="office-365-encryption-for-skype-for-business-onedrive-for-business-sharepoint-online-and-exchange-online"></a><span data-ttu-id="e2f99-103">Шифрование Office 365 для Скайп для бизнеса, OneDrive для бизнеса, SharePoint Online и Exchange Online</span><span class="sxs-lookup"><span data-stu-id="e2f99-103">Office 365 Encryption for Skype for Business, OneDrive for Business, SharePoint Online, and Exchange Online</span></span>

<span data-ttu-id="e2f99-104">Office 365 — это очень безопасной среде, которая обеспечивает расширенную защиту в нескольких уровней: центра обработки данных физических, безопасности, безопасность сети, безопасность доступа, безопасность приложений и безопасность данных.</span><span class="sxs-lookup"><span data-stu-id="e2f99-104">Office 365 is a highly secure environment that offers extensive protection in multiple layers: physical data center security, network security, access security, application security, and data security.</span></span>

## <a name="skype-for-business"></a><span data-ttu-id="e2f99-105">Skype для бизнеса</span><span class="sxs-lookup"><span data-stu-id="e2f99-105">Skype for Business</span></span>
<span data-ttu-id="e2f99-p101">Скайп для клиента бизнес-данных может храниться в rest в виде файлов или презентации, отправленные участниками собрания. Сервер веб-конференций выполняет шифрование данных клиента, с помощью AES с 256-разрядный ключ. Зашифрованные клиента данные хранятся в общей папке. Каждой части пользовательских данных шифруется с помощью различных случайным 256-разрядный ключ. Если часть данных клиента общих в конференции, сервер веб-конференций этот параметр клиентов конференц-связи для загрузки данных зашифрованные клиента с помощью HTTPS. Он отправляет соответствующий ключ для клиентов, чтобы данные клиента могут быть расшифрованы. Сервер веб-конференций также выполняет проверку подлинности клиентов конференц-связи перед тем как разрешить клиентам доступа к данным клиента конференции. При присоединении к веб-конференции, каждый клиент конференц-связи устанавливает диалоговое окно SIP с компонентом фокус конференц-связи, выполняется внутри интерфейсного сервера через TLS, сначала. Фокус конференц-связи передает клиенту конференции файл cookie проверки подлинности, созданные сервером веб-конференций. Клиент конференц-связи осуществляет подключение к серверу веб-конференций, презентацию файла cookie проверки подлинности на проверку подлинности на сервере.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p101">Skype for Business customer data may be stored at rest in the form of files or presentations that are uploaded by meeting participants. The Web Conferencing server encrypts customer data using AES with a 256-bit key. The encrypted customer data is stored on a file share. Each piece of customer data is encrypted using a different randomly generated 256-bit key. When a piece of customer data is shared in a conference, the Web Conferencing server instructs the conferencing clients to download the encrypted customer data via HTTPS. It sends the corresponding key to clients so that the customer data can be decrypted. The Web Conferencing server also authenticates conferencing clients before it allows the clients access to conference customer data. When joining a Web conference, each conferencing client establishes a SIP dialog with the conferencing focus component running inside the front-end server over TLS first. The conferencing focus passes to the conference client an authentication cookie generated by the Web Conferencing server. The conferencing client then connects to the Web Conferencing server presenting the authentication cookie to be authenticated by the server.</span></span>

## <a name="sharepoint-online-and-onedrive-for-business"></a><span data-ttu-id="e2f99-116">SharePoint Online и OneDrive для бизнеса</span><span class="sxs-lookup"><span data-stu-id="e2f99-116">SharePoint Online and OneDrive for Business</span></span>
<span data-ttu-id="e2f99-p102">Все файлы клиентов в SharePoint Online защищены уникальный, каждого файла Нажимаемые всегда являются исключительно для одного клиента. Ключи либо создан и управляемых службой SharePoint Online либо при использовании ключа клиента создаются и управляются с клиентами. При загрузке файла шифруются с SharePoint Online внутри контекста запроса загрузки перед отправкой хранилища Azure. При загрузке файла SharePoint Online извлекает зашифрованные клиента данные из хранилища Azure на основе идентификатора уникальных документов и расшифровывает данные клиента перед отправкой для пользователя. Хранилище Azure имеет возможность расшифровать, или даже определить или понять данные клиента. Шифрование и дешифровка произойти же систем, обеспечивающие изоляция клиента, которые являются Azure Active Directory и SharePoint Online.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p102">All customer files in SharePoint Online are protected by unique, per-file keys that are always exclusive to a single tenant. The keys are either created and managed by the SharePoint Online service, or when Customer Key is used, created and managed by customers. When a file is uploaded, encryption is performed by SharePoint Online within the context of the upload request, before being sent to Azure storage. When a file is downloaded, SharePoint Online retrieves the encrypted customer data from Azure storage based on the unique document identifier and decrypts the customer data before sending it to the user. Azure storage has no ability to decrypt, or even identify or understand the customer data. All encryption and decryption happen in the same systems that enforce tenant isolation, which are Azure Active Directory and SharePoint Online.</span></span>

<span data-ttu-id="e2f99-p103">Несколько рабочих нагрузок в Office 365 хранения данных в SharePoint Online, включая группами Майкрософт, который хранит все файлы в SharePoint Online и OneDrive для бизнеса, который использует SharePoint Online для его хранилища. Все данные клиента, хранящимся в SharePoint Online зашифрован (с помощью одного или нескольких ключей AES 256-разрядная версия) и распределены по центра обработки данных, как показано ниже. (Каждый шаг этот процесс шифрования является FIPS 140-2 уровня 2 проверены. Для получения дополнительных сведений о соответствия требованиям FIPS 140-2 см [соответствия требованиям FIPS 140-2](https://docs.microsoft.com/previous-versions/sql/sql-server-2008-r2/bb326611(v=sql.105))).</span><span class="sxs-lookup"><span data-stu-id="e2f99-p103">Several workloads in Office 365 store data in SharePoint Online, including Microsoft Teams, which stores all files in SharePoint Online, and OneDrive for Business, which uses SharePoint Online for its storage. All customer data stored in SharePoint Online is encrypted (with one or more AES 256-bit keys) and distributed across the datacenter as follows. (Every step of this encryption process is FIPS 140-2 Level 2 validated. For additional information about FIPS 140-2 compliance, see [FIPS 140-2 Compliance](https://docs.microsoft.com/previous-versions/sql/sql-server-2008-r2/bb326611(v=sql.105)).)</span></span>
- <span data-ttu-id="e2f99-p104">Каждый файл разбивается на один или несколько фрагментов, в зависимости от размера файла. Каждый блок шифруется с помощью собственный уникальный AES 256-разрядный ключ.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p104">Each file is split into one or more chunks, depending on file size. Each chunk is encrypted using its own unique AES 256-bit key.</span></span>
- <span data-ttu-id="e2f99-129">При обновлении файла обновление обрабатывается так же, как: изменение разбивается на один или несколько фрагментов, а каждый блок зашифрован с помощью отдельной уникальный ключ.</span><span class="sxs-lookup"><span data-stu-id="e2f99-129">When a file is updated, the update is handled in the same way: the change is split into one or more chunks, and each chunk is encrypted with a separate unique key.</span></span>
- <span data-ttu-id="e2f99-130">Следующие фрагменты – файлы, части файлов и обновление дельты – сохраняются как большие двоичные объекты в хранилище Azure случайным образом распределены по несколько учетных записей хранилища Azure.</span><span class="sxs-lookup"><span data-stu-id="e2f99-130">These chunks – files, pieces of files, and update deltas – are stored as blobs in Azure storage that are randomly distributed across multiple Azure storage accounts.</span></span> 
- <span data-ttu-id="e2f99-131">Набор ключи шифрования для этих фрагментов данных клиента является шифрование.</span><span class="sxs-lookup"><span data-stu-id="e2f99-131">The set of encryption keys for these chunks of customer data is itself encrypted.</span></span>
   - <span data-ttu-id="e2f99-132">Клавиши, которые используются для шифрования большие двоичные объекты хранятся в SharePoint Online базы данных контента.</span><span class="sxs-lookup"><span data-stu-id="e2f99-132">The keys used to encrypt the blobs are stored in the SharePoint Online Content Database.</span></span>
   - <span data-ttu-id="e2f99-p105">База данных контента был защищен с шифрование статических и элементов управления доступа к базе данных. Шифрование выполняется с помощью [Прозрачное шифрование данных](https://docs.microsoft.com/sql/relational-databases/security/encryption/transparent-data-encryption-tde) в [Базе данных SQL Azure](https://docs.microsoft.com/azure/sql-database/sql-database-technical-overview). (База данных azure SQL — это служба общего назначения реляционной базы данных в Microsoft Azure, которая поддерживает структуры, такие как реляционные данные, JSON Пространственные и XML). Эти секреты, на уровне службы для SharePoint Online, не на уровне клиента. Эти секреты (иногда называется главного ключа) хранятся в отдельных безопасное хранилище вызывается ключом хранилища. TDE обеспечивает безопасность на уровне rest для активной базы данных и резервных копий баз данных и журналов транзакций.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p105">The Content Database is protected by database access controls and encryption at rest. Encryption is performed using [Transparent Data Encryption](https://docs.microsoft.com/sql/relational-databases/security/encryption/transparent-data-encryption-tde) (TDE) in [Azure SQL Database](https://docs.microsoft.com/azure/sql-database/sql-database-technical-overview). (Azure SQL Database is a general-purpose relational database service in Microsoft Azure that supports structures such as relational data, JSON, spatial, and XML.) These secrets are at the service level for SharePoint Online, not at the tenant level. These secrets (sometimes referred to as the master keys) are stored in a separate secure repository called the Key Store. TDE provides security at rest for both the active database and the database backups and transaction logs.</span></span> 
   - <span data-ttu-id="e2f99-p106">При клиенты предоставляет дополнительный ключ, ключа клиента хранится в хранилище ключа Azure и служба использует ключ шифрования с ключом клиента, который используется для шифрования ключом сайта, который затем используется для шифрования ключей уровня файла. По сути введенным новой иерархии ключей при клиент предоставляет ключ.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p106">When customers provide the optional key, the customer key is stored in Azure Key Vault, and the service uses the key to encrypt a tenant key, which is used to encrypt a site key, which is then used to encrypt the file level keys. Essentially, a new key hierarchy is introduced when the customer provides a key.</span></span>
- <span data-ttu-id="e2f99-140">Повторно сборки в файл карты хранится в базе данных контента, а также зашифрованных разделов, отдельно от основного ключа, необходимые для их расшифровки.</span><span class="sxs-lookup"><span data-stu-id="e2f99-140">The map used to re-assemble the file is stored in the Content Database along with the encrypted keys, separately from the master key needed to decrypt them.</span></span>
- <span data-ttu-id="e2f99-p107">Каждой учетной записи хранилища Azure имеет свои собственные уникальные учетные данные на тип доступа (чтение, запись, перечисление и удаление). Каждый набор учетных данных остается в secure Store ключа и регулярно обновляются. Как описано выше, существует три различных типа хранилищ, каждый из которых различные функции.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p107">Each Azure storage account has its own unique credentials per access type (read, write, enumerate, and delete). Each set of credentials is held in the secure Key Store and is regularly refreshed. As described above, there are three different types of stores, each with a distinct function:</span></span>
- <span data-ttu-id="e2f99-p108">Данные клиента сохраняется как зашифрованные большие двоичные объекты в хранилище Azure. Ключ для каждого блока данных клиента шифруются и отдельно хранятся в базе данных контента. Данные клиента самого содержит понять как расшифрован.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p108">Customer data is stored as encrypted blobs in Azure storage. The key to each chunk of customer data is encrypted and stored separately in the Content Database. The customer data itself holds no clue as to how it can be decrypted.</span></span>
- <span data-ttu-id="e2f99-p109">База данных контента представляет собой базу данных SQL Server. Он содержит карты, необходимых для поиска и заново собрать данные клиента большие двоичные объекты, которые проводятся в хранилища Azure, а также клавиш, необходимым для шифрования эти большие двоичные объекты. Тем не менее набор разделов является зашифрован (как описано выше) и элемент хранится в отдельном хранилище ключа.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p109">The Content Database is a SQL Server database. It holds the map required to locate and reassemble the customer data blobs held in Azure storage as well as the keys needed to encrypt those blobs. However, the set of keys is itself encrypted (as explained above) and held in a separate Key Store.</span></span>
- <span data-ttu-id="e2f99-p110">Хранилище ключ физического разделения из хранилища базы данных контента и Azure. Он содержит учетные данные для каждого контейнера хранилища Azure и главный ключ набор зашифрованных разделов, хранящихся в базе данных контента.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p110">The Key Store is physically separate from the Content Database and Azure storage. It holds the credentials for each Azure storage container and the master key to the set of encrypted keys held in the Content Database.</span></span>

<span data-ttu-id="e2f99-p111">Каждый из этих трех компонентов хранения – Azure BLOB-хранилища базы данных контента и хранилище ключа – физического разделения. Информация, хранящаяся в одном из компонентов не может использоваться самостоятельно. Без доступа для всех трех пользователь не сможет получить с помощью клавиш блоки, расшифровки клавиши, чтобы сделать их можно использовать, связать ключи с их соответствующие блоки, расшифровки каждого блока или восстановить документ из его составные блоки.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p111">Each of these three storage components – the Azure blob store, the Content Database, and the Key Store – is physically separate. The information held in any one of the components is unusable on its own. Without access to all three, it is impossible to retrieve the keys to the chunks, decrypt the keys to make them usable, associate the keys with their corresponding chunks, decrypt each chunk, or reconstruct a document from its constituent chunks.</span></span>

<span data-ttu-id="e2f99-155">BitLocker сертификаты, которые защитить томами физических дисков на компьютерах в центре обработки данных, хранимых в безопасное хранилище (SharePoint Online секретный внутренний), который был защищен с помощью ключа фермы.</span><span class="sxs-lookup"><span data-stu-id="e2f99-155">BitLocker certificates, which protect the physical disk volumes on machines in the datacenter, are stored in a secure repository (the SharePoint Online secret store) that is protected by the Farm key.</span></span>

<span data-ttu-id="e2f99-156">Ключи TDE, защиты ключей в больших двоичных объектов, хранятся в двух местах:</span><span class="sxs-lookup"><span data-stu-id="e2f99-156">The TDE keys that protect the per-blob keys are stored in two locations:</span></span>
- <span data-ttu-id="e2f99-157">Безопасный репозитория, который содержит сертификаты BitLocker и был защищен с ключом фермы; и</span><span class="sxs-lookup"><span data-stu-id="e2f99-157">The secure repository, which houses the BitLocker certificates and is protected by the Farm Key; and</span></span>
- <span data-ttu-id="e2f99-158">В безопасное хранилище управляется базы данных SQL Azure.</span><span class="sxs-lookup"><span data-stu-id="e2f99-158">In a secure repository managed by Azure SQL Database.</span></span>

<span data-ttu-id="e2f99-p112">Учетные данные, используемые для доступа к контейнерам хранилища Azure также хранятся в SharePoint Online секрет хранения и передать каждой ферме SharePoint Online, при необходимости. Эти учетные данные, хранилища Azure SAS подписей с отдельные учетные данные, используемые для чтения или записи данных и политики применен, чтобы они auto-истечения срока действия каждые 60 дней. Другие учетные данные используются для чтения или записи данных (но не оба) и фермах SharePoint Online не назначаются разрешения на перечисление.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p112">The credentials used to access the Azure storage containers are also held in the SharePoint Online secret store and delegated to each SharePoint Online farm as needed. These credentials are Azure storage SAS signatures, with separate credentials used to read or write data, and with policy applied so that they auto-expire every 60 days. Different credentials are used to read or write data (not both) and SharePoint Online farms are not given permissions to enumerate.</span></span>

> <span data-ttu-id="e2f99-p113">Примечание для Office 365 США правительственных клиентов, больших двоичных объектов, хранятся в хранилище государственных организаций США Azure. Кроме того персонала Office 365, в частности экранироваться ограничен доступ к SharePoint Online ключи в государственном секторе США Office 365. Azure правительства США персоналу нет доступа к хранилищу SharePoint Online ключа, который используется для шифрования данных больших двоичных объектов.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p113">NOTE For Office 365 U.S. Government customers, data blobs are stored in Azure U.S. Government Storage. In addition, access to SharePoint Online keys in Office 365 U.S. Government is limited to Office 365 staff that have been specifically screened. Azure U.S. Government operations staff do not have access to the SharePoint Online key store that is used for encrypting data blobs.</span></span>

<span data-ttu-id="e2f99-165">Дополнительные сведения о шифрование данных в SharePoint Online и OneDrive для бизнеса см [в OneDrive для бизнеса и SharePoint Online](https://technet.microsoft.com/en-us/library/dn905447.aspx).</span><span class="sxs-lookup"><span data-stu-id="e2f99-165">For more information about data encryption in SharePoint Online and OneDrive for Business, see [Data Encryption in OneDrive for Business and SharePoint Online](https://technet.microsoft.com/en-us/library/dn905447.aspx).</span></span>

### <a name="list-items-in-sharepoint-online"></a><span data-ttu-id="e2f99-166">Элементы списка в SharePoint Online</span><span class="sxs-lookup"><span data-stu-id="e2f99-166">List Items in SharePoint Online</span></span>
<span data-ttu-id="e2f99-p114">Элементы списка, более мелких блоках клиента, данные, которые создаются точечную или, можно дополнительно динамически live в пределах сайта, такие как строк в списке, созданные пользователем, отдельных записей в SharePoint Online блог или записи в SharePoint Online вики-страниц. Элементы списка хранятся в базе данных контента (базы данных SQL Azure) и защищенные с помощью TDE.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p114">List Items are smaller chunks of customer data that are created ad-hoc or that can live more dynamically within a site, such as rows in a user-created list, individual posts in a SharePoint Online blog, or entries within a SharePoint Online wiki page. List items are stored in the Content Database (Azure SQL Database) and protected with TDE.</span></span>

## <a name="encryption-of-data-in-transit"></a><span data-ttu-id="e2f99-169">Шифрование транзитных данных</span><span class="sxs-lookup"><span data-stu-id="e2f99-169">Encryption of data in transit</span></span>
<span data-ttu-id="e2f99-170">В OneDrive для бизнеса и SharePoint Online используются два сценария для входящих и исходящих данных в центрах данных.</span><span class="sxs-lookup"><span data-stu-id="e2f99-170">In OneDrive for Business and SharePoint Online, there are two scenarios in which data enters and exits the datacenters.</span></span>
- <span data-ttu-id="e2f99-p115">**Клиент обмена данными с сервером** - связи в OneDrive для бизнеса через Интернет с помощью подключения SSL или TLS. Все SSL подключений с помощью клавиш 2048 бит.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p115">**Client communication with the server** - Communication to OneDrive for Business across the Internet uses SSL/TLS connections. All SSL connections are established using 2048-bit keys.</span></span>
- <span data-ttu-id="e2f99-p116">**Перемещение данных между центрами обработки данных** — основная причина для перемещения данных между центрами обработки данных — это географическая репликации возможность аварийного восстановления. Например дельты хранилища больших двоичных объектов и журналы транзакций SQL Server бюро по этой канала. Хотя эти данные уже передаются с помощью частной сети, дальнейшей защищен с помощью шифрования лучшие в своем классе.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p116">**Data movement between datacenters** - The primary reason to move data between datacenters is for geo-replication to enable disaster recovery. For instance, SQL Server transaction logs and blob storage deltas travel along this pipe. While this data is already transmitted by using a private network, it is further protected with best-in-class encryption.</span></span>


## <a name="exchange-online"></a><span data-ttu-id="e2f99-176">Exchange Online</span><span class="sxs-lookup"><span data-stu-id="e2f99-176">Exchange Online</span></span>
<span data-ttu-id="e2f99-p117">Exchange Online использует BitLocker для всех данных почтового ящика и конфигурации BitLocker представлено в [BitLocker для шифрования](office-365-bitlocker-and-distributed-key-manager-for-encryption.md). Шифрование на уровне службы шифрует все данные почтовых ящиков на уровне почтового ящика.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p117">Exchange Online uses BitLocker for all mailbox data, and the BitLocker configuration is described in [BitLocker for Encryption](office-365-bitlocker-and-distributed-key-manager-for-encryption.md). Service-level encryption encrypts all mailbox data at the mailbox level.</span></span> 

<span data-ttu-id="e2f99-p118">В дополнение к шифрование службы Office 365 поддерживает ключа клиента, которая построена на основе службы шифрования. Ключ клиента это управляемых Microsoft ключа для Exchange Online службы шифрования, который также является на стратегия корпорации Майкрософт. Этот метод шифрования предоставляет усиления защиты, не предоставляемые BitLocker, поскольку он обеспечивает разделение администраторов серверов и ключи шифрования, необходимые для расшифровки данных, а шифрование применяется непосредственно к данным (в контрастность с BitLocker применяется шифрование на том логический диск) остается зашифрованные данные клиента скопирован с сервера Exchange.</span><span class="sxs-lookup"><span data-stu-id="e2f99-p118">In addition to service-encryption, Office 365 supports Customer Key, which is built on top of service-encryption. Customer Key is a Microsoft-managed key option for Exchange Online service encryption that is also on Microsoft's roadmap. This method of encryption provides increased protection not afforded by BitLocker because it provides separation of server administrators and the cryptographic keys necessary for decryption of data, and because the encryption is applied directly to the data (in contrast with BitLocker, which applies encryption at the logical disk volume) any customer data copied from an Exchange server remains encrypted.</span></span>

<span data-ttu-id="e2f99-p119">Область для Exchange Online службы шифрования — пользовательских данных, которые хранятся в rest в Exchange Online. (Скайп для бизнеса хранятся почти все вызванные пользователем содержимого в Exchange Online почтового ящика пользователя и поэтому наследует функции шифрования службы версией Exchange через Интернет).</span><span class="sxs-lookup"><span data-stu-id="e2f99-p119">The scope for Exchange Online service encryption is customer data that is stored at rest within Exchange Online. (Skype for Business stores nearly all user-generated content within the user’s Exchange Online mailbox and therefore inherits the service encryption feature of Exchange Online.)</span></span>
